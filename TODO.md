# TODO - Eisenhower Matrix Task Manager (Frontend UI)

- [x] Create model + enums + task class (`lib/models/task_models.dart`)
- [x] Create themes (light/dark) (`lib/themes/app_theme.dart`)
- [x] Create reusable widgets: TaskTile + TaskQuadrantTile (`lib/widgets/task_tile.dart`, `lib/widgets/task_quadrant_tile.dart`)
- [ ] Create screens: Home, Add Task, Edit Task, Settings
- [ ] Replace `lib/main.dart` with navigation + in-memory task state + theme mode toggle
- [ ] Run `flutter analyze` and fix any remaining compile issues (no terminal output used here; rely on IDE diagnostics)

